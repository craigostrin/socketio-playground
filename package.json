{
  "name": "socket.io-playground",
  "version": "0.0.1",
  "description": "",
  "license": "MIT",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint"
  },
  "private": true,
  "devDependencies": {
    "turbo": "^1.10.12"
  },
  "dependencies": {}
}
